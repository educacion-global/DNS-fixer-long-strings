# DNS-fixer-long-strings
sometimes google dns do not work with long strings TXT records. # DNS-fixer-long-strings  Split long DNS TXT / DKIM records into DNS-friendly segments (≤255 bytes each) and produce output ready to paste into Google Domains or other registrars.  Features - Byte-aware splitting (handles multi-byte UTF-8 characters safely) - Returns quo
